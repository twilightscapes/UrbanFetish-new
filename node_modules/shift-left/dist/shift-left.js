"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.shiftLeft = void 0;
function shiftLeft(stringParts, ...variables) {
    if (stringParts[0][0] !== "\n") {
        throw new Error(shiftLeft `
        shiftLeft expects template strings beginning on a new line.

        Example usage:

        shiftLeft\`
          This is {
            a multiline {
              message
            }
          }
        \`
      `);
    }
    const whiteSpaceMatch = stringParts[0].match(/^\s+/);
    const s = stringParts.reduce((bigString, stringPart, index) => `${bigString}${stringPart}${variables[index] || ``}`, ``);
    if (whiteSpaceMatch) {
        return s
            .split("\n")
            .slice(0)
            .map((line) => line.slice(whiteSpaceMatch[0].length - 1))
            .join("\n");
    }
    return s;
}
exports.shiftLeft = shiftLeft;
//# sourceMappingURL=shift-left.js.map